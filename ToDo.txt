TO-DO / NEXT SESSION
====================

WHERE THINGS STAND NOW (2026-08-26)   <<< READ THIS FIRST; blocks below are older
  - ENGINEERING IS VIRTUALLY DONE. All three models (XGBoost W/D/L, Poisson/
    Dixon-Coles, Monte Carlo), the bookmaker benchmark, the 4-tournament
    backtests, and the live real-data webapp are built + verified + pushed.
  - NEW THIS SESSION — TEST SUITE (committed + pushed to `webapp`, 77562f1):
      * tests/ — one file per feature/component (the classic layout), 98 tests,
        run with `make test`. Tests the PIECES that make up the models (each
        feature, weight, math helper), NOT the trained model as a black box.
      * Core suite is DB-FREE + offline (~1.1s) on tiny hand-built fixtures in
        tests/conftest.py (make_matches / make_squads factories). The two real-
        data Germany-vs-San-Marino checks (rolling_form + a real Dixon-Coles fit)
        are @pytest.mark.needs_db and AUTO-SKIP when data/statxi.db is absent
        (conftest hook); they share one session-scoped real_matches load.
      * Standing policy added to agents.md ("## Testing"): every NEW feature ships
        a companion tests/test_<feature>.py asserting a real oracle, DB-free by
        default, proven to bite (mutation-check). Full write-up in methodology.md.
      * Removed the stale empty src/tests/ placeholder. Also committed Liam's
        Projektbeschreibung draft PDF.
  - WHAT'S ACTUALLY LEFT: (a) the written Projektbeschreibung (Jan 2027), (b) the
    poster + presentation (Tomás), (c) Jugend forscht registration (Nov 2026) —
    i.e. writing + paperwork; and OPTIONAL polish: the fitted penalty-shootout
    model (replaces the ~50/50 coin flip), the hypothetical Euro 2028 run, and the
    stage-aware model router stub (all still "optional / do-not-build-yet").

--- SNAPSHOT (2026-08-13; superseded by the block above) ---

WHERE THINGS STAND NOW (2026-08-13)   <<< blocks below are older
  - MODELS — all built, verified, benchmarked (details in DONE blocks + methodology.md):
      * XGBoost Win/Draw/Loss (match + squad + FIFA-rating features; walk_forward backtest).
      * Poisson / Dixon-Coles scoreline (Phases 1-5); beats XGBoost on the broad block.
      * Monte Carlo tournament simulator (WC2022 validated).
      * Bookmaker-odds benchmark (so both halves of the judges' hook exist).
  - WEBAPP — a NEW real-data single-page app is LIVE, on branch `webapp` (this branch).
    Run it with `make webapp` -> http://127.0.0.1:5001/. Four views:
      * Landing (#/): what / how / why + an honest reference-only warning.
      * Predict (#/predict): pick date + two teams -> W/D/L shown as a RANGE
        (Poisson <-> XGBoost, hover a number for each model) + most-likely scoreline
        + expected goals + rolling form + head-to-head (both teams). Remembers your
        last-run match for the session, so Detail -> back doesn't blank the form.
      * Detail (#/detail): all three W/D/L sources (XGBoost / Poisson / bookmaker) +
        scoreline heatmap + raw feature values. Reached via the "Full analysis" link.
      * Scorecard (#/scorecard): the backtest — see SECTION 2 below.
    Flag catalog (238/311 real flags). Old feature-explorer kept at /legacy.
    Data layer isolated in webapp/static/js/api.js; endpoints in webapp/app.py
    (/api/predict returns poisson+xgb; /api/detail adds bookmaker+grid+features;
    /api/scorecard returns the walk_forward backtest).
  - SECTION 2 — BACKTEST SCORECARD page: DONE (2026-08-13). New SPA route #/scorecard
    ("Does it actually work?"): the walk_forward backtest surfaced as (1) a per-
    tournament table (Euro 2016 / WC 2018 / Euro 2020 / WC 2022 / Euro 2024: model acc
    vs favourite acc vs log-loss), (2) pooled model vs naive-favourite vs no-skill
    (acc / log-loss / Brier), (3) model vs bookmaker on the odds-covered subset
    (n=153). Winning cell in each face-off is bolded; verdict lines are generated FROM
    the numbers, not hardcoded. Two honest framing callouts: (a) ~50% is not a coin-flip
    (3-way game, random=33%), (b) it rarely PICKS a draw but P(draw) is well-calibrated
    (~23% vs ~23%) — not a bug. New endpoint /api/scorecard (lazy + cached, ~7s first
    hit). No new modelling — walk_forward.py grew a pure summarize() the CLI report and
    the API BOTH read (CLI output verified byte-identical after the refactor). Full
    write-up in methodology.md ("Frontend: the backtest scorecard page").
  - SECTION 3 — SCORECARD FOLLOW-ONS: DONE (2026-08-16). The two eval sources that were
    CLI-only are now two more sections on #/scorecard, same surface-not-model pattern:
      * "The bigger picture — every match with odds" (broader_eval, n=2184): model vs
        bookmaker vs favourite vs no-skill + the paired-bootstrap CIs (model-book +0.0645
        CI [+.053,+.076] = market sharper; model-base -0.227 CI [-.248,-.206] = beats no-
        skill). The finals bookmaker tile above is n=153; this is the same test at ~14x the
        sample, which is where the CI lives.
      * "Simulating the whole tournament — WC 2022" (montecarlo_eval): top-8 by P(win),
        where the actual champ/runner-up/semis ranked (Argentina 3rd, France 6th), round-
        reach Brier/log-loss vs base-rate (.105/.127, .325/.401 = beats no-skill), + the
        two honest caveats (one tournament; no winner-odds market) verbatim from the CLI.
    NO new modelling: broader_eval.report() + montecarlo_eval.run() refactored to print
    from a pure summarize()/evaluate() the endpoints (/api/broad-eval, /api/montecarlo)
    also call — both `make` outputs verified BYTE-IDENTICAL before/after, endpoints re-
    checked vs the CLI to the decimal, view rendered against live JSON (no undefined).
    Lazy+cached, NOT warmed at startup; the 3 scorecard tiles fetch independently. Full
    write-up in methodology.md ("The last two eval sources, now on the scorecard").
  - SECTION 4 — MONTE CARLO BACKTEST BROADENED TO THE EUROS: DONE (2026-08-16). The MC
    tournament backtest now covers 4 tournaments, not just WC2022: added Euro 2016/2020/
    2024 (src/tournaments/euro20{16,20,24}.json) + full 24-team "best-thirds" support.
      * `make montecarlo-eval` now runs ALL FOUR pooled (main()); run("euro2024") for one.
        POOLED (520 team-round preds): Brier .111 vs .136, log-loss .344 vs .426 = BEATS
        no-skill. Honest spread: model had Spain #1/24 (Euro24 champ) but the underdog
        champs Portugal (Euro16, 5th) + Italy (Euro20, 6th) only mid-table — NOT favourite-
        biased. WC2022 numbers UNCHANGED (Argentina 3/32) — byte-identical throughout.
      * REAL UEFA best-thirds table (user chose full fidelity): 6 groups, top-2 + 4 best
        thirds -> R16; the 15-combo anti-rematch table assigns each qualifying third to a
        group winner. simulate_group now returns 3rd-place + a cross-group key; a vectorised
        euro24 branch ranks the 6 thirds, takes 4, routes via the table (1 mask per combo).
      * KEY FINDING: the editions are NOT one shared bracket. Euro 2020/2024 share the
        modern layout (winners 1B/1C/1E/1F host a third); Euro 2016 DIFFERS (1A/1B/1C/1D)
        with its OWN table. So the table lives IN each config, not as a shared constant.
        Verified: 0 same-group R16 rematches for all 3; each config reproduces the real R16
        draw; groups reconstructed from the DB match graph (not memory). Full write-up in
        methodology.md ("Broadening the backtest to the three Euros").
  - SECTION 5 — 4-TOURNAMENT MC BACKTEST SURFACED ON #/scorecard: DONE (2026-08-25).
    The pooled Monte Carlo backtest (all four editions) is now a UI tile, same surface-
    not-model pattern as SECTIONS 2-4. montecarlo_eval.main()'s pooling was extracted
    into a pure evaluate_all() (per-tournament rows + pooled Brier/log-loss); main() now
    PRINTS from it (make montecarlo-eval verified BYTE-IDENTICAL after the refactor). New
    endpoint /api/montecarlo-all (lazy+cached, reuses the cached cleaned matches) serialises
    it; api.getMonteCarloAll() feeds a new #sc-montecarlo-all tile ABOVE the existing WC2022
    deep-dive (now reframed as "Zooming into one"). Endpoint numbers re-checked vs the CLI
    to the decimal (pooled Brier .1114 vs .1360, log-loss .3444 vs .4257; per-tournament
    Argentina 3/32, Portugal 5/24, Italy 6/24, Spain 1/24). Verdict + spread story
    ("Spain #1 before Euro24, but underdog champ Italy only #6") generated FROM the ranks,
    not hardcoded. WC2022 deep-dive endpoint /api/montecarlo unchanged.
  - NEXT UP (all optional): (a) DONE — see SECTION 5 above;
    (b) a fitted penalty-shootout model (replaces the ~50/50 coin flip) + the hypothetical
    Euro 2028 run (needs a projected draw); (c) the STAGE-AWARE MODEL ROUTER stub (still
    "do NOT build yet"); (d) deadline items (registration Nov 2026, report Jan 2027).
  - BRANCH NOTE: the frontend history sits on BOTH `webapp` and `montecarlo` (shared
    commits, both pushed). `webapp` is the intended home from here on.

--- OLDER SNAPSHOT (kept for history; superseded by the block above) ---

WHERE THINGS STOOD (a prior session)
  - WDL XGBoost model is in good shape: match-level + squad chem/age (TRACK 1) +
    FIFA rating_gap via country pools (TRACK 2) = 20 features. walk_forward.py is
    the honest per-tournament backtest; rating_gap adds a modest, consistent
    lift on a broader 738-match test. Everything is committed + documented.
  - TUNING (branch model-tuning): weighting, hyperparameter AND joint tuners BUILT + run
    on one shared leakage-safe harness (tune_common + tune_weights + tune_hyperparams +
    tune_combined + tune_joint). Finding: NO tuning of weighting or XGB hyperparameters
    is a reproducible source of skill — the hand-tuned baselines are near-optimal.
      * Weighting-only:   val -0.0002, held-out +2.5pp acc, WITHIN NOISE (CI [-.025,+.005]).
      * Hyperparams-only: val -0.0011, held-out +0.7pp acc, WITHIN NOISE (CI [-.026,+.012]).
      * Combined (peeked): best point est (+1.1pp acc) but CI still includes 0.
      * JOINT (clean, no peeking): held-out acc -1.1pp (WORSE), CI [-.030,+.005], noise.
    The +1.1pp (peeked) -> -1.1pp (clean) accuracy REVERSAL is a textbook test-set-peeking
    demo — strong Projektbeschreibung material. Kept ALL baselines; nothing promoted.
    The honest propose-don't-overwrite + bootstrap-CI harness IS the deliverable.
  - BOOKMAKER-ODDS benchmark: DONE this session (committed + pushed, branch
    model-tuning). Both halves of the judges' hook now exist — naive baselines AND
    bookmaker implied probs. Model is well-calibrated, clearly beats no-skill,
    competitive-on-accuracy with the market, honestly a small statistically-real
    margin behind on sharpness. Numbers in the DONE block below.
  - DATA: WC2026 now in results.csv (refreshed from martj42 + final/3rd-place
    scores patched from the patateriedata daily mirror). Odds (Oddsportal, 4
    tournaments + qualifiers) live in the match_odds table AND data/raw/*odds*.csv
    — BOTH gitignored (scraped data + public repo). >>> KEEP A LOCAL BACKUP of the
    4 odds CSVs: a manual browser export, NOT regenerable like the Kaggle
    results.csv. <<<
  - Poisson SCORELINE model: still a STUB — the entire classical-stats half of the
    project is untouched. Now the single biggest remaining gap. Also the agreed
    home for DRAW modelling (see the deferred draw note under the Poisson item).

NEXT SESSION — prioritised
  [~] 1. POISSON SCORELINE MODEL (the classical-stats half). IN PROGRESS on branch
         `poisson` (off consolidated main). src/models/poisson.py built + verified
         through Phase 3; plan file: ~/.claude/plans (lets-start-super-simple...).
         DONE (2026-08-10), each phase verified against real football + methodology.md:
           * Phase 1 — scoreline_matrix + wdl_from_grid (Poisson PMF grid -> H/D/A),
             checked vs hand arithmetic (P(0-0), symmetry, strong-vs-weak).
           * Phase 2 — fit_strengths: attack/defence by MLE (scipy L-BFGS-B, analytic
             gradient, ridge, sum-to-zero centring). Brazil/Spain/Germany top attack;
             Italy top-6 DEFENCE but not top attack (catenaccio signature); San Marino
             etc bottom; home_adv x1.31-1.35.
           * Phase 3 — fit_dixon_coles: recency x importance time weighting + Dixon-
             Coles rho (2-stage profile fit). rho=-0.047 (correct sign); low-score
             cells shift right way; weighting surfaces current form (Spain #1, Morocco
             top-10 D). Holdout WDL log-loss: plain 0.9027 -> +weight 0.8926 -> +DC
             0.8921 (both help, weighting the bigger win).
           * Phase 4 — DONE (2026-08-10). PoissonWDL adapter (src/models/poisson.py)
             rides the existing harness unchanged (pass POISSON_FEATURE_COLUMNS as
             feature_columns; predict_proba ignores the score cols = no leakage).
             src/models/poisson_eval.py runs BOTH models + book on the same covered
             set, paired bootstrap CIs. XGBoost column reproduces broader_eval to 3dp
             (correctness anchor). RESULT — the project's headline:
               BROAD block (n=2184): Poisson 0.795 ll / 0.643 acc BEATS XGBoost 0.826 /
                 0.630 (Poisson-XGB -0.031, CI [-.046,-.018], SIGNIFICANT); Poisson
                 halves the gap to book (+0.033 vs XGB +0.065). Still loses to book
                 (right shape, no leakage).
               FINALS only (n=153): Poisson edges XGB on ll/brier (0.986/0.584 vs
                 1.015/0.606) & is WITHIN NOISE of the bookmaker (+0.0155, CI
                 [-.016,+.046]); but Poisson-XGB now within noise (small n) and XGB
                 wins finals ACCURACY 0.575 vs 0.536. Both beat base-rate.
               WHY: Poisson opponent-adjusts (defence[opp] nets out schedule); XGB's
                 squad/rating features are NaN off finals, so Poisson dominates the
                 broad block but the two converge on finals where XGB has full features.
                 = "right tool per sub-problem", shown with CIs. Full arc in methodology.md.
           * Phase 5 — DONE (2026-08-10). rating_gap covariate ablation on the Poisson
             rate (src/models/poisson_rating_ablation.py; fit_strengths grew an optional
             covariate + PoissonWDL(use_rating=True)). RESULT: NO EFFECT, well-powered —
             plain 0.795/0.643 vs +rating 0.795/0.642; paired lift +0.0001, CI
             [-0.0006,+0.0009], dead on zero. NOT a coverage artefact: 2182/2184 covered
             rows carry rating_gap. WHY: Poisson strengths ALREADY opponent-adjust team
             quality from goals, so a FIFA-rating gap is a redundant proxy (contrast WDL,
             whose goal_trend doesn't opponent-adjust -> rating_gap was a candidate there).
             DECISION: keep the code (use_rating flag, reproducible) but OFF by default;
             plain Dixon-Coles is the model. Plain column also reproduced Phase 4 to 3dp
             (regression check). Full arc in methodology.md.

         >>> Phases 1-5 COMPLETE. The Poisson/Dixon-Coles WDL model is built, verified,
             benchmarked (beats XGBoost on the broad block, competitive on finals), and
             the rating_gap question is settled. What remains on the classical-stats half:

           * MONTE CARLO — DONE (2026-08-12), Phases A-E, plan reports/montecarlo_plan.md.
             src/models/montecarlo.py (sampler + group + knockout + tournament),
             src/tournaments/wc2022.json (hand-encoded structure, COMMITTED as source),
             src/tournaments/__init__.py (loader + validators), src/models/montecarlo_eval.py.
             Reuses the Poisson layer unchanged; numpy only, no new dependency.
               * Phase A — sample_scorelines draws from the DC-corrected GRID (not two
                 independent rng.poisson — that would drop the correction). Verified n=1e6:
                 MC P(H/D/A) == analytic wdl_from_grid within MC std err; rho=0 grid-sampler
                 == naive independent Poisson (independence sanity).
               * Phase B — wc2022.json: 8 groups + full R16->F bracket, DB spelling verified
                 (Iran / United States / South Korea, NOT IR Iran/USA/Korea Republic). Loader
                 validates 32-team/bracket arithmetic + every team resolves to a fit (loud fail).
               * Phase C — simulate_group: 6 round-robin fixtures, 3/1/0, rank pts->GD->GF
                 (documented simplification vs FIFA head-to-head; exact ties broken at random).
                 Vectorised across sims. vs actual WC2022: 10/16 qualifiers matched, all strong
                 favourites correct, misses = the famous upsets (Japan/Morocco/Senegal). Sane.
               * Phase D — simulate_knockout via _play_ties (groups sims by unique matchup,
                 batch-samples, grid cache; drawn tie = ~50/50 shootout coin flip, documented
                 placeholder). simulate_tournament -> per-team P(reach R16/QF/SF/final/win),
                 asserts monotone reach + champion probs sum to 1 + round slot counts. 20k sims
                 in ~0.5s, reproducible under seed. Pre-tournament fit: Brazil/Spain/Argentina/
                 England/Netherlands/France top; actual champ Argentina 3rd, France 6th.
               * Phase E — montecarlo_eval.py: round-reach Brier 0.105 vs base-rate 0.127,
                 log-loss 0.325 vs 0.401 (BEATS no-skill). Limitations stated: 1 tournament
                 (winner calibration unprovable), NO outright-winner odds market to benchmark.
             Full arc in methodology.md ("Monte Carlo tournament simulator"). ToDo items 7-8
             DONE for WC2022; Euro 24-team "best-thirds" format now DONE too (2026-08-16,
             see the SECTION 4 block up top) — backtest covers 4 tournaments pooled.
               - LATER (still): fitted penalty-shootout model replacing the ~50/50 coin flip;
                 hypothetical Euro 2028 run (needs projected draw); surface the 4-tournament
                 MC backtest on the #/scorecard page.
         - DRAWS ARE POISSON'S JOB (investigated 2026-08-05, deferred to here).
           The WDL model almost never predicts draws, but this is NOT a bug to
           "fix" in XGBoost: its draw probabilities are already ~perfectly
           calibrated (mean P(D)=0.233 vs actual draw rate 0.233, tracks the
           diagonal per-bin). It just never picks draw as the argmax because a
           correctly-calibrated P(D) tops out at ~0.389 — even in coin-flip games
           (|P(H)-P(A)|<3%) the actual split is H .336/D .303/A .361, so draw is
           never the plurality. Forcing more draws (class weights / lean-to-draw
           rule) would LOWER accuracy and worsen log-loss — do not do it. Also:
           knockout matches decided on penalties are stored as DRAWS (full-time
           pre-shootout score, e.g. England 1-1 Italy, Argentina 3-3 France), so
           "discard draws for penalty games" is impossible pre-match and wrong
           under the 1X2 target. The real fix = model the score distribution and
           let draws fall out, with the Dixon-Coles low-score correction (which
           exists precisely because independent Poisson under-predicts 0-0/1-1).
           A separate "who advances" (2-way, ~50/50 shootout) target is the home
           for knockout resolution, needed by the Monte Carlo simulator.

  [x] 2. FINISH TRACK 2 rating_gap ABLATION in broader_eval.py — DONE (2026-08-09).
         Added a per-FEATURE-SET ablation to src/models/broader_eval.py: three
         name-listed feature groups (base / squad / rating) asserted to PARTITION
         FEATURE_COLUMNS (fails loudly if a new feature is added un-grouped), a
         loop running the annual walk-forward for 4 sets (base, +squad, +rating,
         full) on the identical covered block, and paired-bootstrap CIs isolating
         each group's marginal log-loss lift (with - without, both alone on base
         and on top of the other group). Rows pair 1:1 because feature columns
         never change WHICH covered rows are scored — asserted via np.array_equal.
         FINDING (n=2184, broad odds-covered block = mostly qualifiers/Nations
         League): the scratchpad 738-match "+2.3-2.5pp rating_gap" DOES NOT
         reproduce as a significant margin here — every contrast is WITHIN NOISE:
           rating_gap|no squad      -0.0025  CI [-0.0069, +0.0020]
           rating_gap|squad present +0.0010  CI [-0.0031, +0.0052]
           squad|no rating          -0.0021  CI [-0.0053, +0.0010]
           squad|rating present     +0.0014  CI [-0.0015, +0.0044]
         Point estimates lean the right way alone (rating/squad each ~-0.002 ll)
         but flip sign on top of each other; all CIs straddle 0. Honest read: on
         the WIDE block the lift is marginal/noise, consistent with the tournament
         backtest CIs. The earlier +2.3-2.5pp was block-specific, not general.
         The ablation is now a committed, reproducible module, not a one-off.

  [ ] RESEARCH STUB (later): STAGE-AWARE MODEL ROUTER for the webapp "AI insight".
         Idea (from the Phase 4 finding that model strength is block-dependent):
         serve Poisson WDL on most matches (it's significantly sharper on the broad
         block) and hand over to XGBoost on finals/semis + any category where XGB
         empirically exceeds Poisson — show the BETTER model per match type, not one
         model everywhere. Written up as a HYPOTHESIS in methodology.md (Poisson
         section, "Design idea"). DO NOT build yet — open questions first:
           - Router boundary MUST be a leakage-safe PRE-MATCH rule (tournament stage,
             or better: whether the row even has squad/rating features populated),
             fixed on a validation split. NEVER "pick whichever model won this match"
             = test-set peeking (cf. the +1.1->-1.1pp tuning reversal). Reproducible
             from kickoff-time info only.
           - Evidence is thin + metric-dependent: on finals (n=153) XGB wins ACCURACY
             (0.575 vs 0.536) but Poisson still leads log-loss/brier (within noise).
             Need a bigger finals sample + a pre-registered metric to decide.
           - Test a CALIBRATED BLEND (weighted avg of the two prob vectors, weight fit
             on validation) as the baseline the hard router must beat.
           - Probable real driver = FEATURE AVAILABILITY, not "finals" per se (XGB
             closes the gap exactly when squad/rating cols are non-NaN) — router might
             key on that directly. Map per-stage/favourite/feature-populated wins with
             paired CIs before building. Ties into the "AI insight" frontend item
             (build only with real validated numbers, never a routed/blended fake).

  [ ] Lower priority (when convenient):
        - [x] WC2026 3rd-place score (France 4-6 England) CONFIRMED CORRECT by
              Liam (watched live, 2026-08-09). No longer a data-integrity question.
        - Human review of ratings_match_review_queue.csv (153 medium-confidence
          links, gitignored/regenerable) — Liam's call each, per no-fabrication.
        - (c) captain-marker fix: build_squad_schema.py clean_player_name() only
          strips literal "(captain)", not the "(c)" abbreviation some pre-2016
          Wikipedia pages use. Does NOT affect the 5 ratings-linked tournaments.
          Fix whenever squad scraping is next touched.
        - Tournament STAGE weighting (group vs knockout) — distinct from the
          competition-tier importance weights already built; still not done.

DONE THIS SESSION (2026-08-12/13): FRONTEND REDESIGN -> LIVE REAL-DATA WEBAPP
  - Replaced the old feature-explorer with a 100%-new single-page app served by
    Flask at / (webapp/static/ + templates/index.html). HTML skeleton + CSS (two
    themes: Soft Bright default + Refined Dark toggle, persisted) + small classic-
    script JS modules on one window.StatXI namespace (flags/api/theme/router +
    views landing/predict/detail + app). No build step; classic scripts (not ES
    modules) so it works served AND via file://. Design chosen via throwaway
    mockups in webapp/previews/ (kept as record). Old explorer preserved as
    templates/legacy_explorer.html, served at /legacy.
  - LANDING (#/): what/how/why + honest "reference model, not betting advice / not
    a live Euro 2028 forecast" warning.
  - PREDICT (#/predict): pick date + two teams -> four boxes load independently
    with spinners. W/D/L shown as a RANGE spanning the two models (Poisson<->
    XGBoost, e.g. "22-32%") with a hover tooltip breaking out each model; scoreline
    + expected goals (Poisson lambda) + rolling form + head-to-head (both teams).
    Dropped the tournament-scoped squad-chemistry tile for date-scoped Expected
    Goals so the whole page is date-scoped (no date->tournament mapping needed).
  - DETAIL (#/detail?home=..&away=..&date=..): "for the nerds" page reached from a
    "Full analysis" link. All three W/D/L sources side by side (XGBoost, Poisson,
    bookmaker), the full scoreline probability HEATMAP, and the raw feature values.
  - FLAG CATALOG (static/js/flags.js): team name -> ISO 3166-1 alpha-2 -> emoji at
    runtime; England/Scotland/Wales subdivision emoji; 238/311 teams get a real
    flag, the 73 footballs are all genuinely flag-less (CONIFA/unrecognised/
    historical/Kosovo/N.Ireland). Verified vs the live team list, no typos.
  - BACKEND (webapp/app.py):
      * /api/predict returns BOTH models (poisson + xgb) for the range.
      * NEW /api/detail: poisson + xgb + bookmaker + 6x6 scoreline grid + features.
      * XGBoost is DATE-AWARE: retrained per "as of" date on the cached
        training_matrix via walk_forward._fit_before (leakage-free, ~1.3s, cached
        per date) -> directly comparable to the date-aware Poisson. One-match
        inference builds a single 21-feature row (squad+rating NaN for a
        hypothetical, importance=1.0); class order [H,D,A] from LABEL_TO_INT.
      * Leakage guard reconfirmed: compute_weights weights but does NOT filter
        future matches, so the endpoint slices date<ref itself (like walk_forward).
      * Bookmaker lookup from the matrix's book_ph/pd/pa (either orientation);
        only real played matches have a line.
      * /api/rolling-form + /api/h2h are now date-aware (optional ?date=).
  - Committed + pushed to montecarlo: c0d401c (docs sync) -> ece46e3 (SPA) ->
    cd4e4af (flags) -> 9113f6f (detail page + range).
  - Section 2 — the BACKTEST SCORECARD page — DONE 2026-08-13 (see the WHERE-THINGS-
    STAND block up top for the full description). Remaining webapp surfacing: the broad
    odds-covered block (broader_eval, n=2184 + CI) and the Monte Carlo round-reach
    backtest (montecarlo_eval, WC2022) — both still only in the CLI, not the UI.
  - Open tradeoff still open: the XGBoost single-match row (predict/detail endpoints)
    NaNs squad+rating and assumes importance=1.0 (fidelity vs simplicity) — revisit if
    it distorts predictions. Unrelated to the scorecard, which uses the full matrix.

DONE THIS SESSION (2026-08-07): BOOKMAKER-ODDS BENCHMARK + WC2026 DATA
  - The missing half of the judges' hook. Source: hand-exported from Oddsportal
    (Kaggle/football-data.co.uk are CLUB-only; 538 dead; Odds API paid+partial;
    Oddsportal feed AES-encrypted -> a human browser flat copy-paste is the
    workable, reproducible path). Covers Euro 2020/2024 + WC 2022/2026 +
    qualifiers; Euro2016/WC2018 have NO Oddsportal data (permanent gap).
  - Pipeline: build_odds_schema.py (auto-detecting flat-paste parser -> match_odds
    table) -> src/odds.py (american_to_decimal, devig via multiplicative
    normalisation, team crosswalk w/ the Ireland=Republic trap, date-tolerant join
    + home/away re-orientation) -> build_matrix.py joins book_ph/pd/pa as METADATA,
    NEVER features (leakage guard re-verified). 2184/2205 odds rows resolved.
  - RESULT (broad, n=2184, src/models/broader_eval.py): model log-loss 0.826 vs
    bookmaker 0.762 (model-book +0.0645, CI [+.053,+.076] -> market genuinely
    sharper, the expected honest result); model beats base-rate -0.227 (CI
    [-.248,-.206]). Model acc 0.630 vs book 0.654. Finals-only (n=153): model
    matches book on accuracy (0.575 vs 0.569), book better calibrated.
  - Bookmaker column also added to walk_forward.py + evaluate_wdl.py.
  - Two real bugs found+fixed en route, BOTH caught by numeric sanity checks not
    "it ran": CSS-scraper odds/team misalignment (favourite-accuracy check) and
    bare-American-int parse (draw-calibration check). Full arc in methodology.md.
  - WC2026: results.csv refreshed from martj42; final (Spain 1-0 Argentina) +
    3rd-place (France 4-6 England) patched from patateriedata mirror.
  - Committed + pushed to model-tuning (1b8df1d). Odds CSVs + DB stay gitignored.

DONE PREVIOUS SESSION: XGB HYPERPARAMETER TUNER (Phase B)
  - src/models/tune_hyperparams.py on the shared tune_common harness. 120-trial
    leakage-safe random search over 6 knobs. Result: val -0.0011, held-out +0.7pp
    acc but WITHIN NOISE (CI [-0.026,+0.012]) — kept baseline, nothing promoted.
    Both tuning marginals (weighting + hyperparams) are flat.

RUNNING THE WEBAPP
  - Simplest:  make webapp   (starts Flask on http://127.0.0.1:5001/, no venv
    activation needed).  make kill  frees port 5001 if a stale process holds it.
    (Manual equivalent: source venv/bin/activate; python3 -m webapp.app — port
    5001, not 5000: macOS AirPlay squats on 5000.)
  - It's now the SPA (landing / predict / detail), served from webapp/static/ +
    templates/index.html. Old feature-explorer still at /legacy.
  - If a route 404s / a card loads forever or the model output looks stale: it's
    a STALE server process serving old code (the reloader doesn't always fire, and
    Jinja caches the template in non-debug) — restart it (make kill; make webapp),
    then HARD-refresh the browser (Cmd+Shift+R) to bypass cached static JS/CSS.

DONE THIS SESSION (TRACK 2 — FIFA ratings into the model, via country pools)
  - Correlation de-risk: mean squad `overall` predicts strongly (r=0.42, higher-
    rated team wins 75% of decisive tournament matches).
  - Found + diagnosed the "clouding": naive absolute ratings HURT (0.459); relative
    rating_gap fixed the encoding; adding it to the squad features seemed to cloud
    them. Root cause = DATA SCARCITY, not redundancy (rating vs squad r~0.07; overall
    scale stable across editions; bootstrap CIs showed it was within noise).
  - FIX (partner's idea): ratings aren't limited to 5 tournaments — only LINEUP data
    is. Approximate each country's strength from its tournament-squad player POOL's
    mean overall at the era-correct edition, applied to ANY covered match.
      * src/data_pipeline.py: fifa_edition_for_date() (date->FIFA edition, reproduces
        the tournament map exactly), TOURNAMENT_START_DATE, load_team_pool_ratings().
      * src/features.py: build_pool_index() + team_pool_rating() with a leakage-safe
        first_seen cutoff (a match only sees players known as internationals by its
        own date — verified France's eligible pool grows 22->45 over 2016->2023).
      * src/models/build_matrix.py: swapped tournament-only absolutes for a single
        pool-based rating_gap; absolutes kept as metadata. rating_gap coverage
        281 -> 1081 leakage-free matches; signal holds r=0.42.
  - RESULT: broader 738-match test -> rating_gap +2.3-2.5pp, best log-loss; clouding
    dissolves (all-features 0.520 ~= +rating_gap 0.528). Tournament backtest (281)
    all within noise (CIs ~[0.43,0.56]). Kept all 20 features. wdl_xgb.json retrained
    (2014 split still can't use ratings — expected; all-test 0.579 unchanged).
    walk_forward.py now inherits rating_gap automatically. Full arc in methodology.md.

DONE THIS SESSION (TRACK 1 — squad features wired into the WDL model, v2)
  - src/data_pipeline.py: load_tournament_editions() + match_tournament_edition()
    — the match-row -> tournament-edition glue. Handles the label mismatch
    ("FIFA World Cup" vs "World Cup"), the Euro-2020-played-in-2021 fix, and
    returns None (-> NaN, never fabricated) for non-major / pre-1990 / WC2026.
    6/6 unit checks pass; 875/960 major rows resolve, 1723/1750 team-slots hit
    a squad (27 misses = real name changes: West Germany, Soviet Union/CIS,
    FR Yugoslavia, China PR — all pre-2014, all correctly NaN).
  - src/models/build_matrix.py: 10 squad columns added to FEATURE_COLUMNS
    (home/away x mean_age, squad_size, club_hhi, same_club_pair_ratio,
    largest_club_bloc). Leakage guard re-verified (Germany@WC2014 club_hhi
    0.161 matrix==standalone to 6dp); existing 9 features' NaN counts
    unchanged; squad cols populate on exactly 861 rows, all major (0 leakage).
  - train_wdl.py / evaluate_wdl.py needed NO code change (inherit FEATURE_COLUMNS);
    v2 model retrained + saved. Official (original split): all-test acc 0.580,
    ECE 0.025; MAJOR acc 0.500 vs baseline 0.495 (v1 was 0.492 — now wins).
  - Ran A/B under two splits + a pruning test (scratchpad feature_ablation.py,
    not committed). end-of-2020 split gave the biggest lift (+3.2pp major).
    Dropping squad_size hurt, so all 10 kept. Full write-up in methodology.md.
  - Ran a full WALK-FORWARD backtest (scratchpad walk_forward.py, not committed):
    the +3.2pp does NOT survive rigorous per-tournament retraining — pooled v2
    0.498 vs v1 0.488 (+1.1pp, within noise), log-loss slightly worse. Squad
    features are marginal; walk-forward's value is methodological + unblocking
    TRACK 2. Documented honestly in methodology.md ("Walk-forward reality check").
  - NEXT: promote the walk-forward harness to a committed module (see above).

DONE PREVIOUS SESSION (player-rating matcher, Workstream B/C)
  - link_player_ratings.py — blocking(fifa_version+nationality) -> score
    (name_similarity + exact DOB) -> tier(high/medium/none) matcher. players
    table extended (sofifa_player_id, rating_match_score, rating_match_tier
    — also added to build_squad_schema.py's CREATE TABLE so a future squad
    rebuild doesn't drop the columns, same precedent as transfermarkt_player_id)
  - Coverage: 3,364 players across the 5 in-scope tournaments, 2,851 (84.7%)
    auto-linked at high confidence, 153 to manual review, 360 unmatched
    (no guess made). Full per-tournament table in methodology.md.
  - Verified: Ronaldo (same sofifa_player_id across all 5 tournaments,
    overall/club trajectory exactly matches real history) + Messi (Barca->PSG
    move correctly reflected) + a HARDER Silva test than the original
    Transfermarkt case — 6 distinct Portuguese Silvas, same nationality so
    blocking alone couldn't separate them, all correctly and distinctly
    matched by name+DOB scoring alone
    - side-finding: South Korea/Korea Republic nationality-name mismatch
      (only one found across all 5 tournaments' full country lists)
    - side-finding: WC2018 era-match uses FIFA18 not FIFA19, since FIFA19
      released AFTER the tournament ended (would be hindsight, not prediction)
  - Analysed whether v1 XGBoost is retrain-ready with the new data — it's a
    genuine two-track answer, not a simple yes (see TRACK 1 / TRACK 2 above
    and methodology.md for the full numbers)
  - methodology.md fully updated (matcher + the retrain analysis)

DONE EARLIER SESSION (team chemistry + name-matching string layer)
  - Workstream A: team_chemistry feature + /api/team-chemistry + frontend card
  - Workstream C (string half): normalize_name + name_similarity (rapidfuzz)
  - Fixed requirements.txt merged-line bug; corrected squad count ~380 -> 432
  - Moved webapp to port 5001
  - methodology.md + agents.md updated

DONE EARLIER SESSION (ratings data acquisition, Workstream B)
  - Kaggle auth: legacy kaggle.json in ~/.kaggle/ (the new KGAT_ token format
    isn't supported by kaggle 1.7.4.5 or kagglehub 0.3.13 — both checked,
    both latest on PyPI)
  - Correction: real source is stefanoleone992, not bryanb (bryanb has only
    one single-edition FIFA23 dataset)
  - data/raw/fc24_male_players.csv (96MB, CC0-1.0) — turns out to be a
    superset covering FIFA 15 through FC 24 in one file, not just "2023 +
    fallback". See methodology.md for the full reasoning — the "2023
    baseline" plan is no longer the right approach, each tournament gets its
    own era-correct edition instead.
  - build_ratings_schema.py — player_ratings table built and verified
    (180,021 rows, editions 15-24). Messi/Ronaldo spot-checked directly in
    the table; Silva ambiguity (270 candidates) reproduced from this source
    too.
